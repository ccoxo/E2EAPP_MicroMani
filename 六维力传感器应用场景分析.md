# 六维力传感器应用场景分析

## 结论

在以下三个目录中均发现了与六维力/力矩传感器相关的实现或数据：

| 目录 | 主要用途 | 状态 |
| --- | --- | --- |
| `E:\ICF靶自动装配运动控制系统x64\SerialTest` | 力反馈遥操作、接触/碰撞抑制、X/Z 自动修正、采集与显示 | 已形成闭环控制链路 |
| `E:\自动化实验\SerialTest` | 与 ICF 工程基本同源的实验实现，保留大量 `sixforce.txt` 记录 | 已实际采集并用于运动修正 |
| `E:\fsx\DataCollection` | Qt/NI-DAQ 六维传感器模块，支持置零、时间戳和实时曲线 | 采集模块完整，主界面接入尚不完整 |
| `E:\fsx\DDPG_Pytorch\force*` | 以六维力与位姿数据训练装配策略 | 离线训练存在，在线硬件接口未完成 |

## 传感器的核心作用

六维力传感器的作用是让自动装配系统感知工件接触状态，并根据受力调整运动；系统不再只按预设位置硬性推进。

主要作用如下：

1. **接触与碰撞检测**：利用 `Fx`、`Fy`、`Fz` 判断接触方向和程度。受力增大后，系统降低对应轴的碰撞系数与运动速度，减小过载风险。
2. **装配对位微调**：将传感器受力转换到滑台的 X/Z 坐标，计算位置修正量 `g_modifyPosX`、`g_modifyPosZ`，用于补偿装配偏差。
3. **自适应遥操作**：接触系数和碰撞系数参与速度计算；发生接触时，遥操作轴自动降速，避免继续高速压入。
4. **过程记录与复盘**：保存力、时间、编码器位置和修正量，便于标定、异常分析和示教数据积累。
5. **数据驱动装配学习**：在 DDPG/DMP 项目中，六维力是策略状态输入，用于学习受力条件下的位姿调整动作。

## 已实现的力反馈闭环

`E:\ICF靶自动装配运动控制系统x64\SerialTest` 与 `E:\自动化实验\SerialTest` 中的 `DIALOG8.cpp` 实现了主要闭环。

### 采集与标定

- 使用 NI-DAQ 从 `Dev3/ai0:5` 同步读取 6 路模拟电压。
- 配置采样率为 10 kHz，每采集 1,000 点触发一次回调，控制更新约为 10 Hz。
- 使用固定的 `6 × 6` 标定矩阵将电压转换为六维力/力矩：`Fx`、`Fy`、`Fz`、`Mx`、`My`、`Mz`。
- 对采样结果按 20 点分段平均，以降低噪声。
- 支持零点扣除；代码中还保留了面向实验调试的初始零点参数。

### 受力到运动的转换

1. 主要选取平动力分量计算接触状态。
2. 根据安装角度进行约 32° 的坐标转换，将传感器受力映射至滑台 X/Z 方向。
3. 通过分段标定系数将 X/Z 受力转换为位移修正量。
4. 小于约 `0.02` 的修正量被置零，避免噪声引起抖动。
5. 根据修正量大小设置接触系数和碰撞系数；高碰撞等级下系数可降为 `0.1`。
6. `DIALOG3.cpp` 中的遥操作速度线程将这些系数乘入轴速度；`xmoveFromForce` 和 `zmoveFromForce` 会通过运动控制卡更新 X/Z 目标位置。

因此，该方案属于**基于经验阈值和增益的力反馈柔顺装配**：既能降低碰撞时的速度，也能进行位置补偿。

## 采集、显示与记录场景

工程中存在大量 `force\*sixforce.txt` 文件，表明系统曾持续记录六分量数据。例如，`E:\自动化实验\SerialTest\force` 中存在多批实验记录。

记录内容在不同历史分支中不完全一致：

- 一些文件包含连续六列力/力矩数据；
- `DIALOG8.cpp` 的显示分支还会记录 X/Z 修正量、时间戳和编码器位置；
- 较早的 `DIALOG3.cpp` 记录分支当前只启用了单通道写入，原本的六列原始写入语句被注释。

因此，若这些数据用于训练或统计，应先核对每批日志的列数、单位、采样频率和零点状态。

## fsx 中的传感器模块

`E:\fsx\DataCollection\DataCollection\SixAxisForceSensor.cpp` 将硬件接口封装为 `SixAxisForceSensor`：

- 读取 `Dev3/ai0:5` 六路输入；
- 采样率为 1 kHz，每 100 点更新一次当前六维力；
- 保留原始值、零点值和带时间戳的当前值；
- 通过互斥锁保护跨线程读取；
- 提供 `Initialize()`、`Start()`、`Stop()`、`GetCurrentForce()` 与 `SetZero()` 接口。

`E:\fsx\QSerialTest3.0\QSerialTest\QSerialTest\ForceSensorThread.cpp` 则以约 50 Hz 读取最新力数据并刷新六条实时曲线。

注意：当前 `DataCollection.cpp` 未发现对 `m_forceSensor` 的创建、`Initialize()` 或 `Start()` 调用；因此这部分应视为已经完成的底层采集封装，而非已完整接入主程序的功能。

## 基于六维力的装配学习

`E:\fsx\DDPG_Pytorch\force3`、`force4` 和 `force6` 中存在多种离线学习实验：

- `force4` 从轨迹 CSV 中提取 6 维力数据和 6 维位姿数据；相邻位姿差作为动作。
- 训练奖励惩罚过大的力变化和过大接触力，并偏好特定范围内的接触力。
- `force3` 的状态设计为：5 维当前位置 + 6 维力传感器数据 + 5 维 DMP 参考动作 + 5 维目标误差，共 21 维。
- 目录中已存在轨迹 CSV、训练数据、模型和训练曲线，说明离线训练流程实际执行过。

但 `force4\inference.py` 中的 `read_force_sensor()` 仍返回随机六维数据，机器人动作也只是示例调用。因此该部分目前属于**利用真实或历史轨迹做离线策略研究**，尚未形成真实传感器驱动的在线强化学习控制。

## 使用边界与风险

1. 代码未提供足以确认传感器厂商或型号的有效信息；可确认的是 NI-DAQ 负责采集六路电压，标定矩阵负责解耦。
2. 力与力矩单位未在代码中明确标注，不能直接假设为 N 和 N·m。
3. 实时闭环主要使用平动力分量来修正 X/Z；力矩分量已采集和显示，但未发现其直接参与运动控制。
4. 当前控制为规则/增益式补偿，不是完整的六自由度阻抗或导纳控制；阈值、零点、坐标转换角和比例系数应结合传感器标定报告与现场试验复核。
