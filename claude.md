# Claude Code — MicroMani / AppStation

@AGENTS.md

## 加载与任务执行

以上导入是本项目共享规则的唯一正文；本文件补充 Claude Code 的使用约定，不复制另一套行为准则。所有面向用户的回答、新增注释说明和提交消息描述默认中文，技术标识符保持原样。

- 开始时依据用户目标选择必要源码与测试，复杂任务给出简短计划后执行。普通不确定性通过检查代码、日志或最小实验消除；仅对影响正确性或授权的缺失信息询问。
- 先保留有效会话设置。不要根据任务中的型号名称、截图或旧文档假定账号可访问的模型，也不要自动改 `.claude/settings.json`、个人设置或环境变量。
- 按任务复杂度调整工作粒度和验证深度；`CLAUDE.md` 中的文字不是运行时模型切换开关，推理参数以当前客户端支持为准。
- 技能正文按需加载，长日志保留错误、位置和必要上下文；不为简单编辑启用额外规划、子代理或全量扫描。

## 项目技能与详细内容

| 场景 | Claude Code 技能 | 详细规则 |
| --- | --- | --- |
| UI、状态订阅、相机和操作反馈 | `/micromani-frontend` | [前端](docs/ai/frontend.md) |
| API、配置、LeRobot 数据和策略契约 | `/micromani-backend` | [后端](docs/ai/backend.md) |
| C++、DDS、设备执行与力保护 | `/micromani-hal` | [HAL](docs/ai/hal.md) |

技能由相关任务触发，也可显式调用；调用不扩大操作授权。只读取当前需要的流程。配置迁移、能力缺失或维护这些文件时读 [模型适配与验收](docs/ai/model-adaptation.md)。

## 上下文压缩与恢复

压缩前保留用户目标、已确认授权、当前文件和关键符号、实际测试结果、未解决问题和下一步。恢复后先对照工作区和摘要，继续未完成工作；不要重做已通过的检查或重问已回答的问题。测试输出丢失时标记证据缺失，不编造“通过”。

## 生效确认

在新会话用 `/memory` 检查本文件与 `AGENTS.md` 导入，用技能选择器检查上述入口。若客户端没有发现技能，直接读取对应详细规则；随后按适配文档排查发现机制。仅检查文件格式不能证明模型已经遵循规则。
